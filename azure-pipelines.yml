trigger:
- main

pool:
  vmImage: 'ubuntu-latest'

variables:
  python.version: '3.9'
  


   
stages:
  - stage: build
    displayName: 'Build'
    jobs:
    - job: build
      displayName: 'Build'
      steps:
      - task: Bash@3
        inputs:
          targetType: 'inline'
          script: |
            pwd
            ls
            echo "$(System.DefaultWorkingDirectory)"
      - task: UsePythonVersion@0
        displayName: 'Use Python 3.9'
        inputs:
          versionSpec: '$(python.version)'
      - task: PythonScript@0
        displayName: 'Install dependencies'
        inputs:
          scriptSource: 'inline'
          script: 'pip install'
          arguments: '-r /home/vsts/work/1/s/requirements.txt'